---
- name: Bootstrap WinRM over HTTPS (use only if WinRM is reachable)
  hosts: all
  gather_facts: no
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/vault.yml
  roles:
    - role: bootstrap-winrm